//write a JS function that validates if the user input string is email or not
//use string methods

//write a JS function that count number of vowels used in string
//vowels: aeiou

//Types of Functions
//Default Function
//
//Anonymous Function
//Callback Function

const print = (fullName) => {
    return `Print is ${fullName}`;
};

const mainFn = (fname, lname, cb)=> {
    const  name = 
}