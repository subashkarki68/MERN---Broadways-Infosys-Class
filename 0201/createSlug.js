//Write a JS function to create slug

const generateSlug = (str) => String(str).toLowerCase().replaceAll();
const generateSlugFromArray = (arr) => arr.join("-").toLowerCase();

console.log(generateSlug("Subash Karki?? hello ajshdkj &&& && $%#$@#$@"));

const array = ["Learning", "MERN", "STACK"];
console.log(generateSlugFromArray(array));

32 - 47;
58 - 64;
91 - 96;
